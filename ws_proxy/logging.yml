version: 1
disable_existing_loggers: False
formatters:
  message_only:
    format: "%(message)s"
handlers:
  message_only_stream:
    class: logging.FileHandler
    level: INFO
    formatter: message_only
    filename: keystrokes.log
loggers:
  keystrokes:
    level: INFO
    handlers: [message_only_stream]
    propagate: no